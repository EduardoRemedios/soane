# Fixtures: ACR-V1-001

## Version

v1

## Change Log

- v1 (2026-07-12): Initial Stage F fixture contract.

## Required Families

- natural task query with relevant document matches despite unmatched low-value terms
- zero document and zero memory match returning explicit degraded empty context
- explicit lower-level broad inspection preserving access controls
- separate document and memory budget truncation with stable ordering
- one-hop allowlisted relationship expansion with cycle and non-current targets
- restricted, suppressed, and propagation-blocked targets
- constitutional/canonical role precedence without authority promotion
- concurrent refresh contention with one valid published index
- refresh failure preserving the prior valid index and reporting failure
- JSON and Markdown output stability

## Constraints

Fixtures are local and deterministic. They require no network, external provider, dependency install, credential, persistence layer, or product UI.
